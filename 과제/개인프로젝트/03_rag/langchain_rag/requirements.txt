# Python 3.14 검증 완료 버전 (v2, 2026-06-24)
# 기존 langchain 0.3.x pin 은 tokenizers 빌드 실패로 3.14 미지원 → 1.x 로 상향.

# --- Core LangChain (1.x) ---
langchain==1.3.11
langchain-core==1.4.8
langchain-community==0.4.2
langchain-google-genai==4.2.5
langchain-text-splitters==1.1.2

# --- Vector store (Chroma) ---
langchain-chroma==1.1.0
chromadb==1.5.9

# --- API ---
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# --- Tracing / Evaluation ---
langsmith==0.9.1
# RAGAS 표준 4대 지표 평가. import 시 사라진 langchain_community.chat_models.vertexai 를
# 참조하는 버그가 있어 evaluation/eval_ragas.py 상단에서 shim 으로 우회한다.
ragas==0.4.3
